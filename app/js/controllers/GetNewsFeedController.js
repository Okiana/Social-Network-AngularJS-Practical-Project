'use strict';

SocialNetwork.controller('GetNewsFeedController',
   function ($scope, $rootScope) {
       $rootScope.pageTitle = "Home";




       /*SocialNetwork.controller('postController',
        function($scope, $location, $route, $routeParams, postService, userService, notifyService){
        */



/*
 * getNewsFeed: function (startPostId, pageSize,success, error) {
 var request = {
 method: 'GET',
 url: baseServiceUrl + '/me/feed?PageSize='+ pageSize,
 headers: authentication.getAuthHeaders()
 };
 $http(request).success(success).error(error);
 },
* */


   }
);
